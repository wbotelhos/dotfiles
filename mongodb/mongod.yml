net:
  bindIp: 127.0.0.1
  port: 27.0.0.1

processManagement:
  fork: true
  pidFilePath: '/var/run/mongodb.pid'

storage:
  journal:
    enabled: true

systemLog:
  destination: 'file'
  logAppend: true
  path: '/var/log/mongodb/out.log'
  quiet: false
